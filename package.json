{
  "name": "chat-app",
  "version": "1.0.0",
  "description": "WhatsApp-like chat application using MERN stack",
  "main": "index.js",
  "scripts": {
    "start": "node backend/server.js",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "nodemon backend/server.js",
    "client": "npm start --prefix frontend",
    "build": "npm install && npm install --prefix frontend && npm run build --prefix frontend",
    "setup": "node setup.js",
    "install-all": "npm install && cd frontend && npm install && cd ..",
    "vercel-build": "cd frontend && npm install && npm run build",
    "railway-build": "npm install"
  },
  "keywords": ["chat", "mern", "socket.io", "react"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.0.3",
    "socket.io": "^4.6.1"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "nodemon": "^3.0.2"
  }
}

